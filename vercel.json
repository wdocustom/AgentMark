{
  "version": 2,
  "installCommand": "npm install && npm run build --workspace=@agentmark/shared",
  "builds": [
    {
      "src": "api/index.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/index.ts"
    }
  ]
}
